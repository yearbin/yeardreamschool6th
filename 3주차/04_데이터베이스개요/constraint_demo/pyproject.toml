[project]
name = "constraint-demo"
version = "0.1.0"
description = "제약 조건 비교 시연 — Streamlit 회원가입/로그인 (good.db vs bad.db)"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40.0",
]

[tool.uv]
package = false
